<div class="error-404-page">
  <div class="error-container">
    <div class="error-content">
      <h1 class="error-code">404</h1>
      <h2 class="error-title">Sidan hittades inte</h2>
      <p class="error-description">
        Tyvärr kunde vi inte hitta sidan du letade efter. 
        Möjligtvis har den tagits bort eller så skrev du in en felaktig adress.
      </p>

      <div class="error-actions">
        <a href="/" class="btn btn-primary">Till startsidan</a>
        <a href="/collections" class="btn btn-secondary">Se alla kategorier</a>
      </div>
    </div>

    <div class="error-suggestions">
      <h3>Här är några användbara länkar:</h3>
      <ul>
        <li><a href="/collections/sovrum">Sovrumsförvaring</a></li>
        <li><a href="/collections/badrum">Badrumsförvaring</a></li>
        <li><a href="/collections/kok">Köksförvaring</a></li>
        <li><a href="/collections/vardagsrum">Vardagsrumsförvaring</a></li>
        <li><a href="/collections/tradgard">Trädgårdsförvaring</a></li>
        <li><a href="/collections/kontor">Kontorsorganisering</a></li>
      </ul>
    </div>
  </div>
</div>

<style>
.error-404-page {
  min-height: calc(100vh - 200px);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 4rem 2rem;
  background: linear-gradient(135deg, #0E0E0E 0%, #1A1A1A 100%);
}

.error-container {
  max-width: 800px;
  text-align: center;
  animation: fadeIn 0.6s cubic-bezier(0.16,1,0.3,1);
}

@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.error-content {
  margin-bottom: 4rem;
}

.error-code {
  font-family: 'Instrument Serif', serif;
  font-size: 8rem;
  font-weight: 900;
  background: linear-gradient(135deg, #FF6B35 0%, #FF8C42 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  line-height: 1;
  margin-bottom: 1rem;
}

.error-title {
  font-family: 'Instrument Serif', serif;
  font-size: 2.5rem;
  color: #F0EDE8;
  margin-bottom: 1.5rem;
}

.error-description {
  font-size: 1.125rem;
  color: #B3AFA0;
  line-height: 1.8;
  margin-bottom: 3rem;
}

.error-actions {
  display: flex;
  gap: 1.5rem;
  justify-content: center;
  flex-wrap: wrap;
  margin-bottom: 4rem;
}

.btn {
  padding: 1rem 2.5rem;
  border-radius: 8px;
  font-family: 'Syne', sans-serif;
  font-weight: 600;
  text-decoration: none;
  transition: all 0.3s cubic-bezier(0.16,1,0.3,1);
  font-size: 1rem;
}

.btn-primary {
  background: #FF6B35;
  color: #0E0E0E;
}

.btn-primary:hover {
  background: #FF8C42;
  transform: translateY(-2px);
  box-shadow: 0 8px 24px rgba(255, 107, 53, 0.3);
}

.btn-secondary {
  background: transparent;
  border: 2px solid #FF6B35;
  color: #FF6B35;
}

.btn-secondary:hover {
  background: rgba(255, 107, 53, 0.1);
  border-color: #FF8C42;
  color: #FF8C42;
}

.error-suggestions {
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid rgba(255, 107, 53, 0.2);
  padding: 3rem;
  border-radius: 12px;
  backdrop-filter: blur(10px);
}

.error-suggestions h3 {
  font-family: 'Instrument Serif', serif;
  font-size: 1.5rem;
  color: #F0EDE8;
  margin-bottom: 2rem;
}

.error-suggestions ul {
  list-style: none;
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 1rem;
}

.error-suggestions li a {
  display: block;
  padding: 1rem;
  color: #B3AFA0;
  text-decoration: none;
  border-radius: 8px;
  transition: all 0.3s;
}

.error-suggestions li a:hover {
  background: rgba(255, 107, 53, 0.1);
  color: #FF6B35;
  padding-left: 1.5rem;
}

@media (max-width: 768px) {
  .error-code {
    font-size: 5rem;
  }

  .error-title {
    font-size: 1.75rem;
  }

  .error-description {
    font-size: 1rem;
  }

  .error-actions {
    flex-direction: column;
    gap: 1rem;
  }

  .btn {
    width: 100%;
  }

  .error-suggestions {
    padding: 2rem;
  }

  .error-suggestions ul {
    grid-template-columns: 1fr;
  }
}
</style>
